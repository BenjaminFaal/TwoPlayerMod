namespace Benjamin94
{
    namespace Input
    {
        
        /// <summary>
        /// This enum will be used for determining stick directions
        /// </summary>
        public enum Direction
        {
            Forward, Left, Right, Backward, ForwardLeft, ForwardRight, BackwardLeft, BackwardRight, None,
        }
    }
}