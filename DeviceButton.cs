namespace Benjamin94
{
    namespace Input
    {
        /// <summary>
        /// This enum is used for mapping button indexes to logical naming
        /// </summary>
        public enum DeviceButton
        {
            DPadUp,
            DPadDown,
            DPadLeft,
            DPadRight,
            Back,
            BigButton,
            Start,
            LeftStick,
            RightStick,
            A,
            B,
            X,
            Y,
            LeftShoulder,
            LeftTrigger,
            RightShoulder,
            RightTrigger
        }
    }
}